import Link from "next/link";
import { StatusBadge } from "@/components/admin/StatusBadge";
import { prisma } from "@/lib/db";
export const dynamic = "force-dynamic";

export default async function AdminProductsPage() { const products = await prisma.product.findMany({ orderBy: [{ sortOrder: "asc" }, { updatedAt: "desc" }] }); return <div><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-semibold uppercase tracking-[0.18em] text-[#C8956C]">Products</p><h2 className="mt-2 text-3xl font-bold text-[#3D3330]">Product drafts</h2></div><Link href="/admin/products/new" className="rounded-lg bg-[#C8956C] px-4 py-2 text-sm font-semibold text-white">New product</Link></div><div className="mt-8 overflow-x-auto rounded-xl border border-[#E8DDD4] bg-white"><table className="min-w-full text-left text-sm"><thead className="border-b border-[#E8DDD4] text-[#6B6B6B]"><tr><th className="p-4">Product</th><th className="p-4">Status</th><th className="p-4">Updated</th></tr></thead><tbody>{products.map((product) => <tr key={product.id} className="border-b border-[#F0E9E4]"><td className="p-4"><Link href={`/admin/products/${product.id}`} className="font-semibold text-[#3D3330] hover:text-[#C8956C]">{product.name}<span className="ml-2 text-xs text-[#6B6B6B]">{product.model}</span></Link></td><td className="p-4"><StatusBadge status={product.status} /></td><td className="p-4 text-[#6B6B6B]">{product.updatedAt.toLocaleString()}</td></tr>)}</tbody></table>{products.length === 0 && <p className="p-8 text-sm text-[#6B6B6B]">No products yet.</p>}</div></div>; }
