import { notFound } from "next/navigation";
import { prisma } from "@/lib/db";
import { StatusBadge } from "@/components/admin/StatusBadge";
import { ContentActions } from "@/components/admin/ContentActions";
export default async function AdminProductDetail({ params }: { params: { id: string } }) { const product = await prisma.product.findUnique({ where: { id: params.id }, include: { images: true } }); if (!product) notFound(); return <article><p className="text-sm font-semibold uppercase tracking-[0.18em] text-[#C8956C]">Product</p><div className="mt-2 flex flex-wrap items-center gap-3"><h2 className="text-3xl font-bold text-[#3D3330]">{product.name}</h2><StatusBadge status={product.status} /></div><p className="mt-2 text-[#5C534E]">Model {product.model} · ${Number(product.price).toFixed(2)} · {product.images.length} image(s)</p><dl className="mt-8 grid gap-4 rounded-xl border border-[#E8DDD4] bg-white p-6 sm:grid-cols-2"><div><dt className="text-xs uppercase tracking-wide text-[#9E948A]">Weight capacity</dt><dd className="mt-1 text-[#3D3330]">{product.weightCapacity || "Not provided"}</dd></div><div><dt className="text-xs uppercase tracking-wide text-[#9E948A]">Effective seat width</dt><dd className="mt-1 text-[#3D3330]">{product.seatWidth || "Not provided"}</dd></div><div><dt className="text-xs uppercase tracking-wide text-[#9E948A]">Range</dt><dd className="mt-1 text-[#3D3330]">{product.range || "Not provided"}</dd></div><div><dt className="text-xs uppercase tracking-wide text-[#9E948A]">Product weight</dt><dd className="mt-1 text-[#3D3330]">{product.productWeight || "Not provided"}</dd></div></dl><ContentActions type="products" id={product.id} status={product.status} /></article>; }
