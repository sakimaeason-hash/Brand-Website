import Link from "next/link";
import { StatusBadge } from "@/components/admin/StatusBadge";
import { prisma } from "@/lib/db";
export default async function AdminPromotionsPage() { const promotions = await prisma.promotion.findMany({ orderBy: { startAt: "desc" } }); return <div><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-semibold uppercase tracking-[0.18em] text-[#C8956C]">Promotions</p><h2 className="mt-2 text-3xl font-bold text-[#3D3330]">Scheduled campaigns</h2></div><Link href="/admin/promotions/new" className="rounded-lg bg-[#C8956C] px-4 py-2 text-sm font-semibold text-white">New promotion</Link></div><div className="mt-8 overflow-x-auto rounded-xl border border-[#E8DDD4] bg-white"><table className="min-w-full text-left text-sm"><thead className="border-b border-[#E8DDD4] text-[#6B6B6B]"><tr><th className="p-4">Name</th><th className="p-4">Status</th><th className="p-4">Window</th></tr></thead><tbody>{promotions.map((promotion) => <tr key={promotion.id} className="border-b border-[#F0E9E4]"><td className="p-4 font-semibold text-[#3D3330]">{promotion.name}</td><td className="p-4"><StatusBadge status={promotion.status} /></td><td className="p-4 text-[#6B6B6B]">{promotion.startAt.toLocaleString()} – {promotion.endAt.toLocaleString()}</td></tr>)}</tbody></table>{promotions.length === 0 && <p className="p-8 text-sm text-[#6B6B6B]">No promotions yet.</p>}</div></div>; }
