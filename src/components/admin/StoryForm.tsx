"use client";

import { useState } from "react";
import { MediaUploader, type PendingMedia } from "./MediaUploader";

export function StoryForm() { const [form, setForm] = useState({ displayName: "", location: "", quote: "", productId: "", source: "", tags: "Travel, Comfort", isFeatured: false, sortOrder: "0" }); const [images, setImages] = useState<PendingMedia[]>([]); const [message, setMessage] = useState(""); const [busy, setBusy] = useState(false); const update = (key: string, value: string | boolean) => setForm((current) => ({ ...current, [key]: value })); async function submit(e: React.FormEvent) { e.preventDefault(); setBusy(true); const body = new FormData(); body.set("payload", JSON.stringify({ ...form, tags: form.tags.split(",").map((tag) => tag.trim()).filter(Boolean), sortOrder: Number(form.sortOrder), productId: form.productId || null })); images.filter((item) => !item.error).forEach((item) => body.append("images", item.file)); const response = await fetch("/api/admin/stories", { method: "POST", body }); const result = await response.json(); setMessage(response.ok ? "Draft saved." : result.error || "Unable to save draft"); setBusy(false); } return <form onSubmit={submit} className="max-w-3xl space-y-6"><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium">Display name<input required value={form.displayName} onChange={(e) => update("displayName", e.target.value)} className="mt-1 w-full rounded-lg border border-[#D4CCC5] px-3 py-2" /></label><label className="text-sm font-medium">Location<input value={form.location} onChange={(e) => update("location", e.target.value)} className="mt-1 w-full rounded-lg border border-[#D4CCC5] px-3 py-2" /></label><label className="text-sm font-medium">Source<input value={form.source} onChange={(e) => update("source", e.target.value)} className="mt-1 w-full rounded-lg border border-[#D4CCC5] px-3 py-2" /></label><label className="text-sm font-medium">Product ID<input value={form.productId} onChange={(e) => update("productId", e.target.value)} className="mt-1 w-full rounded-lg border border-[#D4CCC5] px-3 py-2" /></label></div><label className="block text-sm font-medium">Quote<textarea required value={form.quote} onChange={(e) => update("quote", e.target.value)} className="mt-1 min-h-40 w-full rounded-lg border border-[#D4CCC5] px-3 py-2" /></label><label className="block text-sm font-medium">Tags (comma separated)<input value={form.tags} onChange={(e) => update("tags", e.target.value)} className="mt-1 w-full rounded-lg border border-[#D4CCC5] px-3 py-2" /></label><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={form.isFeatured} onChange={(e) => update("isFeatured", e.target.checked)} /> Feature this story</label><MediaUploader value={images} onChange={setImages} /><div className="flex items-center gap-4"><button disabled={busy || images.some((item) => item.error)} className="rounded-lg bg-[#C8956C] px-5 py-3 font-semibold text-white disabled:opacity-50">{busy ? "Saving..." : "Save draft"}</button>{message && <p role="status" className="text-sm text-[#5C534E]">{message}</p>}</div></form>; }
